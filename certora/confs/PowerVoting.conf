{
    "files": [
        "src/PowerVoting.sol",
        "src/MinimalToken.sol:MinimalToken"
    ],
    "link": [
        // PowerVoting.token == address(MinimalToken)
        "PowerVoting:token=MinimalToken"
    ],
    "verify": "PowerVoting:certora/specs/PowerVoting.spec",
    // The contract to consider for invariant functions
    "parametric_contracts": ["PowerVoting"],
    "msg": "PowerVoting solution spec",
    "server": "production"
}